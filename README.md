> this a simple project for applying on fresh laravel.

### Template Quick Start

1. Install `Composer` dependencies.
   
        composer install
        
2. Start the localhost server:
    
        php artisan serve
